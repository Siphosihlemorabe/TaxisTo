[pytest]
# Two suites, each living with the code it covers: the cleaning pipeline
# (standard library only) and the backend (needs backend/requirements.txt
# installed). Both run from the repo root, which is what puts `pipeline` and
# `backend` on the path.
testpaths = pipeline/tests backend/tests
